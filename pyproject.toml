[project]
name = "aw-stats"
version = "0.1.0"
description = "Scripts for collecting and analyzing ActivityWatch usage stats"
authors = [{ name = "Erik Bjäreholt", email = "erik@bjareho.lt" }]
requires-python = ">=3.9"
dependencies = [
    "requests",
    "pandas",
    "matplotlib",
    "click",
]
